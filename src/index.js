// @flow
// @format

import * as React from 'react'
import ReactDOM from 'react-dom'
import App from './App'

const rootNode: React.ElementType = document.getElementById('root')
ReactDOM.render(<App />, rootNode)
